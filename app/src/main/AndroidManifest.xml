<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Izin akses jaringan lokal -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Konfigurasi cleartext HTTP untuk 127.0.0.1 -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.NKJayaNet"
        android:usesCleartextTraffic="true">

        <!-- Splash screen (opsional, jika kamu pakai SplashActivity) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.NKJayaNet"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main WebView Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Foreground service untuk menjalankan server -->
        <service
            android:name=".ServerService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Konfigurasi keamanan jaringan (opsional) -->
        <meta-data
            android:name="android.security.network_security_config"
            android:resource="@xml/network_security_config" />

    </application>
</manifest>
